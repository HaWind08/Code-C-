#include<iostream>
#include<math.h>
using namespace std;

int main()
{
	 x;
	cout << "Nhap x: "; cin >> x;

	double F = (x * x + exp(fabs(x)) + pow(sin(x), 2)) / pow(x * x + 1, 0.2);
	cout << "F= " << F;
	return 0;
}
