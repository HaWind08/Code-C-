#include<iostream>
#include<math.h>
using namespace std;

int main()
{
	int n;
	cout << "Nhap n: "; cin >> n;

	int Nghin = n / 1000;
	int Tram = (n / 100) % 10;
	int Chuc = (n / 10) % 10;
	int Donvi = n % 10;

	if (Nghin > 0)
	{
		cout << Nghin << " nghin ";
	}
	if (Tram > 0)
	{
		cout << Tram << " tram ";
	}
	if (Chuc > 0)
	{
		cout << Chuc << " chuc ";
	}
	if (Donvi)
	{
		cout << Donvi << " don vi ";
	}
}
