#include<iostream>
#include<math.h>
using namespace std;

int main()
{
	int x, n;
	cin >> x >> n;
	if (n % 2 != 0)
	{
		cout << 0;
	}
	else
	{
		double sum = 0;
		for (int i = 2; i <= n; i++)
		{
			sum = sum + (1.0 * pow(x, n) / (1.0 * pow(3, n - 1)));
		}
		sum += (2016 * x);
		cout << sum;
	}
}
